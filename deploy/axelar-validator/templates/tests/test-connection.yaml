apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "axelar-validator.fullname" . }}-test-connection"
  labels:
    {{- include "axelar-validator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "axelar-validator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
